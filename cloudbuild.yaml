steps:
- name: 'gcr.io/cloud-builders/gradle'
  args: ['clean', 'assemble']
  
- name: 'gcr.io/cloud-builders/gradle'
  args: ['test']
  
- name: 'gcr.io/$PROJECT_ID/sonar-scanner:latest'  
  args: 
  - '-Dsonar.host.url=https://az-sonar.ellisdon.com'
  - '-Dsonar.login=f0e0fe18041c87c225c7249212ffe90f298d1e9e'
  - '-Dsonar.projectKey=CompContactGCP'
  - '-Dsonar.sources=.'
  - '-X'
  env:
  - 'SONAR_SCANNER_OPTS=-Djavax.net.ssl.keyStore=/keystore.jks -Djavax.net.ssl.trustStore=/keystore.jks'
  
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/hello-app:$BRANCH_NAME', '.']

- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    echo "Here's a convenient pattern to use for embedding shell scripts in cloudbuild.yaml."
    echo "This step only pushes an image if this build was triggered by a push to master."
    [[ "$BRANCH_NAME" == "master" ]] && docker push gcr.io/$PROJECT_ID/hello-app:$BRANCH_NAME || echo "skipping . . ."
    
    #testing sonarqube connection after disabling tests skiping 
    
    

